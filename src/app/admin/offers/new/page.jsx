import React from "react";
import OfferForm from "../OfferForm";

export const metadata = {
  title: "Add New Offer Banner | Novixa Admin",
  description: "Add a new promotional banner",
};

export default function NewOfferPage() {
  return <OfferForm />;
}
